//Any class, field, method or constructor that has default  modifier is accessible only by classes in the same package. 
	//The default modifier is not used for fields and methods within an interface.
	//here package name is (employee)
	//classes are Employee and EmployeeDemo
	//if Employee class is in different package we cannot access class members of Employee in EmployeeDemo even
	//we can import the Employee class in EmployeeDemo it won't work.
