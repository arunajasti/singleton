package stringMethods;
/* getChars() is used for COPYING String characters to an Array of chars.
 * 
 * Here is the syntax of this method 
public void getChars(int srcBegin, int srcEnd, char[]  dst,  int dstBegin)

Return Value
It does not return any value but THROWS INDEX OUT OF BOUNDS 
if srcbegin is greater than srcEnd then it throws IndexoutofBounds

srcBegin:  index of the first character in the string to copy.
srcEnd : index after the last character in the string to copy.
dst: the destination array.
dstBegin : the start offset in the destination array./The index in Array starting from where the chars will be pushed into the Array

 * */
